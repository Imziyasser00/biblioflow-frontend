FROM node:22-alpine
WORKDIR /app

# Optional: faster installs on Alpine
RUN npm config set fund false && npm config set audit false

EXPOSE 4200
CMD [ "sh", "-lc", "npm ci && npx ng serve --host 0.0.0.0 --port 4200" ]
